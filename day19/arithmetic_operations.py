def make_full_name(first_name, last_name):
    # Combines the two arguments with a space in between
    full_name =first_name+" "+last_name
    return full_name

# The Test: Calling the function with my own name
my_name = make_full_name("Thomas", "Paul")

print(my_name) 